import OrbitingFeatureSection from "@/components/OrbitingFeatureSection/Half";

const Home = () => {
  return (
    <div className="w-full h-full flex items-end">
      <OrbitingFeatureSection />
      {/* <OrbitingFeatureSection1 /> */}
    </div>
  );
};

export default Home;
